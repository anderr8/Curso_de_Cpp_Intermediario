#include <iostream>

using namespace std;

int main()
{
    int idade; // Variável idade
/*
    int idade, idade2, idade3; // Para ter mais que uma variável tem que ser separada por, vírgula.
    int Idade; // A variável Idade e diferente da variável idade porque a linguagem C++ é case sensitive.
    // Toda variável tem que começar sempre com uma letra minuscúla (a z) ou maiúscula (A Z), a variável também pode ser começada com anderline (_).
    int idade910; // Números depois do inicio é aceito números.
*/
    //int 1idade; variável não aceita
    //int i dade; variável não aceita
    idade = 20;
    cout << idade;

    return 0;
}